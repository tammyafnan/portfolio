!(function (e) {
  "use strict";
  e(".preloader-close").length &&
    e(".preloader-close").on("click", function () {
      e(".loader-wrap").delay(200).fadeOut(500);
    }),
    e(window).on("load", function () {
      e(".loader-wrap").length && e(".loader-wrap").delay(1e3).fadeOut(500),
        TweenMax.to(e(".loader-wrap .overlay"), 1.2, {
          force3D: !0,
          left: "100%",
          ease: Expo.easeInOut,
        });
    }),
    jQuery(document).on("ready", function () {
      (jQuery(window).on("scroll", function (e) {
        jQuery(window).scrollTop() < 150
          ? jQuery(".fallow-sticky").removeClass("sticky")
          : jQuery(".fallow-sticky").addClass("sticky");
      }),
      e(".wow").length) &&
        new WOW({
          boxClass: "wow",
          animateClass: "animated",
          offset: 250,
          mobile: !0,
          live: !0,
        }).init();
      jQuery(".fallow-video-popup").magnificPopup({ type: "iframe" }),
        jQuery(".fallow-image-popup").magnificPopup({
          type: "image",
          gallery: { enabled: !0 },
        }),
        jQuery(window).on("scroll", function (e) {
          jQuery(this).scrollTop() > 600
            ? jQuery(".back-to-top").fadeIn(200)
            : jQuery(".back-to-top").fadeOut(200);
        }),
        jQuery(".back-to-top").on("click", function (e) {
          e.preventDefault(),
            (document.body.scrollTop = 0),
            (document.documentElement.scrollTop = 0),
            jQuery("*").animate({ scrollTop: 0 }, 1500);
        }),
        e(".canvas_open").on("click", function () {
          e(".offcanvas_menu_wrapper,.off_canvars_overlay").addClass("active");
        }),
        e(".canvas_close,.off_canvars_overlay").on("click", function () {
          e(".offcanvas_menu_wrapper,.off_canvars_overlay").removeClass(
            "active"
          );
        });
      var a = e(".offcanvas_main_menu"),
        n = a.find(".sub-menu");
      n
        .parent()
        .prepend(
          '<span class="menu-expand"><i class="fa fa-angle-down"></i></span>'
        ),
        n.slideUp(),
        a.on("click", "li a, li .menu-expand", function (a) {
          var n = e(this);
          n
            .parent()
            .attr("class")
            .match(/\b(menu-item-has-children|has-children|has-sub-menu)\b/) &&
            ("#" === n.attr("href") || n.hasClass("menu-expand")) &&
            (a.preventDefault(),
            n.siblings("ul:visible").length
              ? n.siblings("ul").slideUp("slow")
              : (n
                  .closest("li")
                  .siblings("li")
                  .find("ul:visible")
                  .slideUp("slow"),
                n.siblings("ul").slideDown("slow"))),
            n.is("a") ||
            n.is("span") ||
            n.attr("clas").match(/\b(menu-expand)\b/)
              ? n.parent().toggleClass("menu-open")
              : n.is("li") &&
                n.attr("class").match(/\b('menu-item-has-children')\b/) &&
                n.toggleClass("menu-open");
        });
    }),
    /(trident|msie)/i.test(navigator.userAgent) &&
      document.getElementById &&
      window.addEventListener &&
      window.addEventListener(
        "hashchange",
        function () {
          var e,
            a = location.hash.substring(1);
          /^[A-z0-9_-]+$/.test(a) &&
            (e = document.getElementById(a)) &&
            (/^(?:a|select|input|button|textarea)$/i.test(e.tagName) ||
              (e.tabIndex = -1),
            e.focus());
        },
        !1
      );
})(jQuery);
